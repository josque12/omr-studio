# OMR Studio 
Reconocimiento Óptico de Partituras con CRNN + CTC

## Requisitos
- best_model.keras (no incluido por tamaño)
- vocab.json

## Instalar
pip install -r requirements.txt

## Correr
streamlit run app.py